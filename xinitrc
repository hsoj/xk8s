#!/bin/sh

xhost +SI:localuser:$USER >/dev/null
echo "DISPLAY=$DISPLAY"
unset WAYLAND_DISPLAY GDK_BACKEND QT_QPA_PLATFORM CLUTTER_BACKEND \
    SDL_VIDEODRIVER ELM_DISPLAY ELM_ACCEL ECORE_EVAS_ENGINE
export XDG_SESSION_TYPE=x11
exec "$@"
